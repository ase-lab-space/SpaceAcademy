# 第4回 確認課題・宿題の解答例(指導者用)

> このファイルは指導者向けです。学生には配布せず、次回冒頭の答え合わせの参考にしてください。

## 進行上の注意(全体)

- **この回は新しい部品を使いません。** 第1回のLED(GP15)を配線したままの状態で始められます。配線に時間を取られない分、コードの構造に集中させてください。
- **最もつまずくのは第5節②の`ImportError`です。** 原因のほとんどは、`led_utils.py`と`main.py`のどちらかがPC側に保存されている(Pico W本体に保存されていない)ことです。Thonny左側のファイル一覧で、両方が「Raspberry Pi Pico」の側に並んでいるかを確認させてください。
- **確認課題1で、編集が反映されない現象がほぼ確実に起きます。** 生徒が`led_utils.py`に関数を追記して`main.py`を実行しても、一度読み込まれたファイルの内容が使われ続けるためです。本文の第5節③に注意書きを入れていますが、実習中は`Ctrl+D`(ソフトリブート)を先回りして案内すると進行が止まりません。第1回のモーターの件と同じ「マイコンが前の状態を持ち越す」という共通の原因なので、ここで結び付けて説明すると定着します。
- **第6節の方法B(パッケージの管理)は、PC側のネットワークを使います。** Pico W自体のWi-Fi設定は不要です。会場のネットワークでPyPIへの接続が制限されている場合は、方法A(GitHubから手動ダウンロード)に切り替えてください。事前に一度試しておくことを推奨します。

## 第5節③:`import`と`from`の使い分けについて

2つの書き方を出していますが、**どちらが正しいという話ではありません。** 生徒から「どちらを使えばいいのか」と聞かれた場合は、次の観点で答えてください。

- `import led_utils`形式は、呼び出し側を見るだけで関数の出どころが分かります。ファイルが増えてきたときに追いやすくなります。
- `from led_utils import blink_led`形式は記述が短くなります。次節のpicozeroや、実際に公開されているライブラリの多くがこの形で紹介されています。
- 第7節の表で「読み込み方」の行を見比べると、`import bme280`(第5回)と`from picozero import LED`(第6節)が別物ではなく、同じ`import`の2つの書き方であることが確認できます。

なお、本文では扱っていませんが、`from led_utils import *`(すべて取り出す)という書き方も存在します。名前の衝突が起きやすく実務では避けられる書き方なので、生徒から質問が出た場合のみ「そういう書き方もあるが、どの関数が入ってくるか分からなくなるので勧めない」と答える程度で十分です。

## 確認課題1:点滅間隔を引数にする

```py
# led_utils.py に追記
def blink_led_v2(pin_num, times, interval):
    led = Pin(pin_num, Pin.OUT)
    for i in range(times):
        led.value(1)
        time.sleep(interval)
        led.value(0)
        time.sleep(interval)
```

```py
# main.py
import led_utils

led_utils.blink_led_v2(15, 3, 0.1)
```

### 指導のポイント

- `blink_led`との違いは引数が1つ増えただけであることに気づかせてください。「引数を増やす=関数をもっと柔軟にできる」という感覚が今日のねらいです。
- `interval`のデフォルト値を持たせる(`def blink_led_v2(pin_num, times, interval=0.3):`)ところまで踏み込めると発展的です。余裕があれば紹介してください。

## 確認課題2:値を返す関数

模範解答例(どちらか、または生徒が考えたものでよい):

```py
def bigger(a, b):
    if a > b:
        return a
    else:
        return b

def seconds_to_min_sec(total_seconds):
    minutes = total_seconds // 60
    seconds = total_seconds % 60
    return minutes, seconds  # タプルとして2つの値を返す
```

### 指導のポイント

- 「動作をする関数」(`blink_led`)と「値を計算して返す関数」(`add`・`bigger`)の違いを言語化できているかを確認してください。
- `return`の後にコードを書いても実行されない(`return`で関数の実行がそこで終わる)ことに気づく生徒がいれば、積極的に取り上げてください。

## 発展課題:モーター・タクトスイッチの関数化

```py
# led_utils.py とは別に motor_utils.py などを作ってもよい
from machine import Pin
import time

def motor_on(pin_num, seconds):
    motor = Pin(pin_num, Pin.OUT)
    motor.value(1)
    time.sleep(seconds)
    motor.value(0)

def is_button_pressed(pin_num):
    button = Pin(pin_num, Pin.IN, Pin.PULL_DOWN)
    return button.value() == 1
```

### 指導のポイント

- モーターについては、[03_マイコン入門とLチカとモーター.md](../03_マイコン入門とLチカとモーター.md)「3.6 モーターを止めたあと、PCと繋がらなくなったら」の内容を思い出させてください。関数の中で`motor.value(0)`を忘れると、前回と同じ症状が起きます。
- タクトスイッチは[04_タクトスイッチとデジタル入力.md](../04_タクトスイッチとデジタル入力.md)のプルダウン抵抗の設定を思い出す必要があります。忘れている生徒には該当ページを見返させてください。

## picozeroとの比較

`picozero.Button`は`is_pressed`というプロパティ、`when_pressed`/`when_released`というイベントハンドラを持っています。自作の`is_button_pressed`(呼ぶたびに読みに行く方式)との違い、特に「呼ばれるまで待つ」動き方(イベント駆動)は今日の範囲を超えるので、深入りせず「便利な書き方が用意されている例」として紹介する程度で十分です。

## 宿題:picozeroのクラス探し

想定される答えの例:

| クラス | 用途 | CanSatでの用途例(生徒の発想を尊重してください) |
| --- | --- | --- |
| `Button` | ボタン・スイッチの読み取り | 着地検知スイッチ、モード切り替えスイッチ |
| `Motor` | DCモーターの正転・逆転・速度制御 | パラシュート展開機構、ホイールの駆動 |
| `Buzzer` | ブザーを鳴らす | 電源投入音、エラー通知音 |
| `DistanceSensor` | 超音波距離センサ | 着地高度の推定 |
| `Servo` | サーボモーターの角度制御 | カメラの向き調整、展開機構 |

正解を1つに絞る必要はありません。「名前から用途を推測し、CanSatに結びつけて考える」という思考の練習が目的です。次回冒頭で2〜3人に発表してもらうと良いでしょう。

---

## 実機での動作確認チェックリスト(公開前の確認用)

本文中のコードは机上で確認済みですが、実機での確認が済んでいません。撮影とあわせて、次の順に確認してください。★印は、資料作成時に文献・公式ドキュメントを根拠に記述した箇所で、特に実機での確認が必要です。

| # | 確認内容 | 該当箇所 |
| --- | --- | --- |
| 1 | GP15のLEDで`blink_led(3)`と`blink_led(5)`が意図通り動く | 第2節 |
| 2 | `time.sleep(0.3)`を`0.5`に変えると、両方の呼び出しに反映される | 第2節 |
| 3 | `led_utils.py`をPico W本体に保存できる | 第5節① |
| 4 | `import led_utils`→`led_utils.blink_led(15, 3)`で点滅する | 第5節② |
| 5 | `from led_utils import blink_led`→`blink_led(15, 3)`でも同じく点滅する | 第5節③ |
| 6 | ★`led_utils.py`を編集後に`main.py`を実行すると変更が反映されず、`Ctrl+D`後に反映される | 第5節③の⚠️ |
| 7 | ★ツール→パッケージの管理…で`picozero`を検索・インストールできる(Thonnyのバージョンにより画面表記が異なる可能性あり) | 第6節 方法B |
| 8 | `LED(15).blink(on_time=0.3, off_time=0.3, n=3)`で点滅する | 第6節 |
| 9 | ★`from picozero import pico_led`が、Pico WのオンボードLEDで動作する | 第6節の💡 |

### 確認結果が本文と食い違った場合

- **6が再現しない場合**(Thonnyが実行のたびに自動でソフトリブートする設定になっている等)は、第5節③の⚠️を「環境によっては起きる」という書き方に緩めてください。
- **7で画面の名称が違う場合**は、本文の手順(「ツール→パッケージの管理…」「PyPIで検索」)を実際の表記に合わせて修正してください。
- **9が動作しない場合**は、第6節の💡を削除するか、外部LED(GP15)を使う案内に差し替えてください。Pico WのオンボードLEDは無線チップ経由で制御される仕様のため、ライブラリ側の対応状況に依存します。
